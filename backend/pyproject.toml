[project]
name = "agent-piano-backend"
version = "0.1.0"
description = "Local-first backend for Agent Piano"
requires-python = ">=3.11"
dependencies = [
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
